#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;
    vector<int> pos(n+1), ids(n+1);

    for (int i = 1; i <=n; i++) { cin >> pos[i]; }
    for (int i = 1; i <= n; i++) { cin >> ids[i]; }

    //then we shuffle till we see
    return 0;
}